projects: [{ pid, name, description, sessions: [{ sid, time, label }] }];

// loop through all the sessions and calculate the time

[
  {
    pid: 1,
    name: "project 1",
    description: "string",
    sessions: [{ sid: 1, time: 1000, label: "string" }],
  },
];

// add sessions
